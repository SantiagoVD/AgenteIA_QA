export type AnalysisStatus = "QUEUED" | "PROCESSING" | "COMPLETED" | "FAILED";
export interface Evidence { relativePath: string; startLine?: number; endLine?: number; description: string; evidenceType: string; }
export interface Finding { guidelineId: string; title: string; domain: string; source: string; status: string; priority: string; confidence: string; reasoningSummary: string; recommendation?: string; evidence: Evidence[]; }
export interface Profile { languages: string[]; frameworks: string[]; hasFrontend: boolean; hasBackend: boolean; hasTests: boolean; hasCI: boolean; hasDocker: boolean; projectType: string[]; databases: string[]; testingFrameworks: string[]; filesDetected: number; cloudProviders: string[]; hasInfrastructure: boolean; hasAuthentication: boolean; coverage: "not available"; signals: { hasEnvExample: boolean; hasPotentialSecrets: boolean; hasCorsSignal: boolean; packageManagers: string[]; documentationFiles: number; configFiles: number; }; }
export interface Metrics { totalGuidelines: number; compliant: number; partiallyCompliant: number; nonCompliant: number; noEvidence: number; notApplicable: number; verifiedComplianceScore: number | null; evidenceCoverage: number | null; }
export interface Report { analysisId: string; repository: { url: string; name: string; branch?: string; commit?: string }; profile: Profile; findings: Finding[]; metrics: Metrics; executiveSummary: string; createdAt: string; }
export interface AnalysisState { analysisId: string; repositoryUrl: string; status: AnalysisStatus; phase: string; error?: string; report?: Report; }
