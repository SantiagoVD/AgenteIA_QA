import { environment } from "../config/environment.ts";
export class QwenClient { private readonly timeoutMs = Number(process.env.OLLAMA_TIMEOUT_MS ?? 45000); async listModels(): Promise<string[]> { const response=await fetch(`${environment.ollamaBaseUrl}/api/tags`,{signal:AbortSignal.timeout(this.timeoutMs)}); if(!response.ok)throw new Error(`Ollama no responde: HTTP ${response.status}`); const data=await response.json() as {models?:Array<{name:string}>}; return (data.models??[]).map(x=>x.name); } async chat(messages:Array<{role:string;content:string}>):Promise<string>{const response=await fetch(`${environment.ollamaBaseUrl}/api/chat`,{method:"POST",headers:{"Content-Type":"application/json"},signal:AbortSignal.timeout(this.timeoutMs),body:JSON.stringify({model:environment.ollamaModel,messages,stream:false,think:false,format:"json",options:{temperature:0.05,num_predict:600,num_ctx:4096}})});if(!response.ok){const detail=await response.text();if(response.status===404&&detail.includes("model"))throw new Error(`El modelo ${environment.ollamaModel} no está disponible. Ejecute: ollama pull ${environment.ollamaModel}`);throw new Error(`Ollama no pudo generar una respuesta: HTTP ${response.status}`);}const data=await response.json() as {message?:{content?:string}};if(!data.message?.content)throw new Error("Ollama respondió sin contenido.");return data.message.content;} }
