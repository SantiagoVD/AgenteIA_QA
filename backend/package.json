{
  "name": "agenteia-qa-backend",
  "version": "0.1.0",
  "private": true,
  "description": "AgenteIA_QA: análisis de cumplimiento QA de repositorios mediante evidencia estática y agentes locales.",
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env --experimental-strip-types --watch src/api/analyses/server.ts",
    "start": "node --env-file=.env --experimental-strip-types src/api/analyses/server.ts",
    "build": "tsc --noEmit",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "ingest": "node --experimental-strip-types scripts/ingest-qa.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
